-- Initialize PostgreSQL database for <%= projectName %>
-- This script runs when the PostgreSQL container starts for the first time

-- Create database if it doesn't exist
SELECT 'CREATE DATABASE <%= projectName %>'
WHERE NOT EXISTS (SELECT FROM pg_database WHERE datname = '<%= projectName %>')\gexec

-- Connect to the database
\c <%= projectName %>;

-- Create extensions if needed
CREATE EXTENSION IF NOT EXISTS "uuid-ossp";
CREATE EXTENSION IF NOT EXISTS "pgcrypto";

-- Create a dedicated user for the application (optional)
-- CREATE USER nestkick_user WITH PASSWORD 'nestkick_password';
-- GRANT ALL PRIVILEGES ON DATABASE <%= projectName %> TO nestkick_user;
-- GRANT ALL PRIVILEGES ON ALL TABLES IN SCHEMA public TO nestkick_user;
-- GRANT ALL PRIVILEGES ON ALL SEQUENCES IN SCHEMA public TO nestkick_user;

-- Set default privileges for future tables
-- ALTER DEFAULT PRIVILEGES IN SCHEMA public GRANT ALL ON TABLES TO nestkick_user;
-- ALTER DEFAULT PRIVILEGES IN SCHEMA public GRANT ALL ON SEQUENCES TO nestkick_user; 